@inject Presentation.Models.Common.Security.SessionContext context
@{ var name = context.Current() != null ? context.Current().Name : string.Empty; }
@{
    ViewData["Title"] = "_Footer";
}
<!--Double navigation-->
<header>
    <!-- Navbar -->
    <nav class="navbar fixed-top FMCDemo-gradient navbar-toggleable-md navbar-expand-lg scrolling-navbar double-nav">
        <!-- SideNav slide-out button -->
        <div class="float-left">
            <a href="#" data-activates="slide-out" class="button-collapse black-text"><i class="fas fa-bars"></i></a>
        </div>
        <!-- Breadcrumb-->
        <div class="breadcrumb-dn mr-auto">
            <p>FMCSA Demo Panel </p>
        </div>
        <ul class="nav navbar-nav nav-flex-icons ml-auto">

            <li class="nav-item">
                <a class="nav-link" href="@Url.Action("Logout", "Account")"><i class="fas fa-user"></i> <span class="clearfix d-none d-sm-inline-block">Logout</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-cogs"></i>Setting
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="@Url.Action("ResetPassword", "Account")">Change Password</a>
                </div>
            </li>
        </ul>
    </nav>
    <!-- /.Navbar -->
</header>
<!--/.Double navigation-->
